import { GenerationRegistryPanel } from "../../components/generation-registry/generation-registry-panel";

export default function GenerationRegistryPage() {
  return (
    <main>
      <GenerationRegistryPanel />
    </main>
  );
}
